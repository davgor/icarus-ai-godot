# Backlog

Work that is not an active epic slice, but must not disappear into “Out of scope” footnotes.

| Folder | Owns |
| --- | --- |
| [`deferred/`](deferred/) | Explicit deferment tickets (with link back to the epic / decision that deferred them) |

Execution-order epics stay in [`../epics/`](../epics/). Feature order stays in [`../feature-list.md`](../feature-list.md).
